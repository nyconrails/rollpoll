%h1 All RollPoll Users

%table.table
  %thead
    %tr
      %th Username
      %th Questions Asked
      %th Votes Cast
      %th{width: '30%'} Actions

  - @users.each do |user|
    %tr
      %td= user.username
      %td= user.questions_count.to_s
      %td= user.votes_count.to_s
      %td
        = link_to 'Show', admin_user_path(user), class: 'btn btn-primary'
        = link_to 'Edit', edit_admin_user_path(user), class: 'btn btn-warning'
        = link_to 'Destroy', admin_user_path(user), :confirm => 'Are you sure?', :method => :delete, class: 'btn btn-danger'
